import{describe,it,expect}from'vitest';import{cases}from'./data';import{advance,createSession,scoreCase}from'./engine';describe('clinical engine',()=>{it('advances clock deterministically',()=>{const c=cases[0];const s=createSession(c,'study');expect(advance(s,c,120).time).toBe(120)});it('rewards appropriate actions',()=>{const c=cases[0];let s=createSession(c,'study');const before=scoreCase(s,c);s={...s,asked:[c.questions[0].q],ordered:[c.exams[0].name],applied:['o2','abx']};expect(scoreCase(s,c)).toBeGreaterThan(before)})});